import {Layout} from "antd";

const FooterComponent = () => {
    return (
        <Layout.Footer style={{background: 'rgba(0, 37, 73, 0.8)', height: '27.6rem', color: 'white'}}>
            footer
        </Layout.Footer>
    );
};

export default FooterComponent;